added server
server now orchestrates all controllers (backend controllers)
no longer one controller per instance, instead 1 controller for all active accounts
notification observer
rentalAction and listingActivationAction
